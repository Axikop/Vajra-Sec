# ── Web + API ──────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2
requests==2.34.2

# ── Scraping / HTML parsing ────────────────────────────────
beautifulsoup4==4.14.3
feedparser==6.0.12

# ── Scheduler ──────────────────────────────────────────────
schedule==1.2.2

# ── External APIs (web search) ─────────────────────────────
tavily-python==0.7.24

# ── Reporting ──────────────────────────────────────────────
reportlab==4.5.1

# ── Local LLM + embeddings (for FofaGPT RAG) ───────────────
sentence-transformers==5.5.1
numpy==2.4.6

# ── Optional: legacy fofabot Playwright path (no longer used)
# Uncomment only if you want the old browser-based scraper.
# playwright==1.60.0
